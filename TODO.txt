Faire sous forme de classes un peu comme le Hardware-Java?

Initialiser plutôt pointeurs-tableaux avec 
a = [1,2,3]
p_a = a.ctypes.data_as(ctypes.POINTER(ctypes.c_double))
?->ce qui est fait actuellement est peut-être équivalent...



OLD

Hardwarex.py : mem leak?->dans matlab on utilisait clear, d'ailleurs il en manque peut-être dans les boucles...->non le MATLAB c'est bon en fait a priori, et le Python a un garbage collector...
